# GeoML-Hub TLS Configuration Example
# 复制此文件为 .env 并根据您的环境修改配置

# =============================================================================
# Docker Remote Connection with TLS
# =============================================================================

# Docker服务器地址（TCP连接用于远程Docker）
DOCKER_MS_HOST=tcp://your-docker-host:2376

# 启用TLS验证
DOCKER_MS_TLS_VERIFY=true

# 方法1: 使用证书目录（推荐）
# 证书目录路径，包含 ca.pem, cert.pem, key.pem 文件
DOCKER_MS_CERT_PATH=/path/to/docker/certs

# 方法2: 使用单独的证书文件路径
# DOCKER_MS_CA_CERT=/path/to/ca.pem
# DOCKER_MS_CLIENT_CERT=/path/to/cert.pem
# DOCKER_MS_CLIENT_KEY=/path/to/key.pem

# Docker API超时时间(秒)
DOCKER_MS_TIMEOUT=60

# =============================================================================
# Model Service TLS Configuration
# =============================================================================

# 是否为模型服务启用TLS/HTTPS
SERVICE_TLS_ENABLED=true

# 服务TLS证书文件路径
SERVICE_TLS_CERT_PATH=/path/to/service/cert.pem

# 服务TLS私钥文件路径
SERVICE_TLS_KEY_PATH=/path/to/service/key.pem

# 服务域名（用于生成HTTPS URL）
SERVICE_DOMAIN=your-domain.com

# =============================================================================
# 其他相关配置
# =============================================================================

# 服务端口范围
SERVICE_PORT_START=7000
SERVICE_PORT_END=8000

# Docker镜像名称
DOCKER_IMAGE_NAME=geoml-service:latest

# 默认资源限制
DEFAULT_CPU_LIMIT=0.5
DEFAULT_MEMORY_LIMIT=512Mi